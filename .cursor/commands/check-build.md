# Check Build

## Purpose
Verifies that the project builds successfully with `yarn build` and outputs build results.

## Usage
Run this skill after making code changes to ensure that the build process completes without errors. This is a crucial step before running tests or submitting pull requests.